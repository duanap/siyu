# 架构与产品决策

## ADR-001：模块化单体

状态：已确认
决定：NestJS 单体按业务模块拆分，Worker 独立进程，共用领域服务。
原因：当前规模不需要微服务，优先保证一致性、测试和部署简单。

## ADR-002：金额使用整数分

状态：已确认
决定：数据库和内部 API 使用整数分。
原因：避免浮点计算误差。
禁止：FLOAT、DOUBLE 和 JavaScript 浮点累计财务金额。

## ADR-003：账本与私有数据双重隔离

状态：已确认
决定：普通账目以账本隔离；工资和个人借贷以用户隔离。
原因：情侣共同记账不等于共享全部个人财务。

## ADR-004：特殊业务通过来源键关联普通账目

状态：已确认
决定：工资、借贷处理和周期实例生成账目时使用 `source_type + source_id` 唯一关联。
原因：防止重复入账并支持追踪来源。

## ADR-005：周期任务使用执行实例

状态：已确认
决定：周期规则与每期实例分表，`rule_id + scheduled_date` 唯一。
原因：支持重试、确认、跳过、错误记录和幂等。

## ADR-006：Ant Design Vue 是基础组件而非完整视觉

状态：已确认
决定：表单和通用交互使用 Ant Design Vue，核心手机页面和卡片二次封装或自定义。
原因：避免手机端变成缩小的管理后台。

## ADR-007：工资余额按发薪周期

状态：暂定已确认
决定：默认从工资到账日至下一预计发薪日前一天。
原因：比自然月更符合“这次工资还剩多少”的用户认知。

## ADR-008：管理后台不直接改财务明细

状态：已确认
决定：后台以状态、关系、任务和审计查询为主。
原因：降低隐私、误操作和内部越权风险。

## ADR-009：SIYU 品牌与工程命名

状态：已确认
决定：品牌唯一事实来源为 `docs/product/BRAND_IDENTITY.md`；根包名 `siyu`，服务和包使用
`siyu` / `@siyu` 前缀，既有数据库业务表名和 `/api/v1` 路径不因品牌迁移而改变。
原因：统一产品展示和工程标识，同时避免无业务收益的兼容性破坏。

## ADR-010：Prisma 特殊约束由受审迁移 SQL 固化

状态：已确认
决定：Prisma Schema 作为模型基线；部分唯一索引、CHECK 和复合归属外键在 create-only 初始迁移中
以 PostgreSQL SQL 固化，并由一次性测试数据库自动验证。
原因：这些约束无法全部由 Prisma Schema 元数据直接表达，但不能降级为仅应用层校验。

## ADR-011：客户端幂等键作用域

状态：已确认
决定：客户端幂等键按“用户 + 操作 + Key”隔离；相同载荷重放原结果，不同载荷返回 409。
原因：允许不同用户或不同操作安全复用客户端生成值，同时阻止单个操作重复写入。

## ADR-012：双认证与可撤销会话

状态：已确认
决定：保留 QQ OAuth 并增加邮箱密码认证；邮箱与 QQ 身份不自动合并。Access Token 短时有效，
Refresh Token 以数据库摘要记录、每次轮换并在重放时撤销会话。认证首次建号继续原子初始化个人账本。
原因：满足基础账号能力，同时避免自动身份合并导致账号接管，并为禁用、退出和改密提供服务端撤销边界。

## ADR-013：最小关联表 RBAC

状态：已确认
决定：角色、权限、用户角色和角色权限使用关联表；默认注册仅授予 USER，ADMIN 仅由受控运维流程授予。
原因：管理端权限必须来自数据库和服务端 Guard，且需支持后续扩展而不暴露客户端提权入口。

## ADR-014：情侣账本退出、转移与解散

状态：已确认
决定：普通成员可以退出；所有者不能直接退出，必须先把所有权原子转移给另一名有效成员，或由所有者解散账本。解散只撤销访问并结束成员和邀请状态，历史财务数据保留用于追溯。
原因：情侣账本必须始终有明确责任人，且关系变化不能破坏历史财务记录或产生短暂无所有者、双所有者状态。

## ADR-015：情侣邀请单活与一次性凭证

状态：已确认
决定：每个情侣账本同一时间只保留一个有效待处理邀请；新建邀请撤销旧邀请，接受后撤销其他待处理邀请。数据库只保存凭证摘要，明文凭证仅在创建响应中返回一次；创建请求使用用户、操作和幂等键隔离。
原因：减少多邀请码并发加入的歧义，避免凭证泄露，并满足重复点击和网络重试时不重复创建邀请。

## ADR-016：自定义分类按账本作用域隔离

状态：已确认
决定：系统分类全局可用；个人自定义分类仅用于个人账本；情侣自定义分类归情侣账本，双方共享使用，创建者可管理自己的分类，账本所有者可管理全部情侣分类。历史账目保持分类展示语义，成员退出后不再授予分类权限。
原因：避免将个人分类和使用习惯无意共享给情侣成员，同时保证共同账目分类口径一致。

## ADR-017：系统分类采用全局模板、账本内实例

状态：已确认
决定：默认系统分类以版本化 `templateKey` 维护全局模板目录，但实际 Category 记录必须按账本实例化；
初始化以 `(ledgerId, templateKey)` 保证幂等。系统分类内容不可由用户修改，OWNER 可排序和启停；
情侣 MEMBER 继续按 ADR-016 管理自己创建的自定义分类，不具备全局排序和系统分类操作权限。
原因：账本实例能直接建立分类、账本和类型的数据库边界，允许各账本独立排序/启停，又不牺牲默认目录升级和初始化幂等。

## ADR-018：Entry 使用不可变创建哈希、乐观锁和来源维护边界

状态：已确认
决定：手工 Entry 以版本化规范载荷的 SHA-256 `createRequestHash` 判断创建重放，该字段创建后不可变；
PATCH 和 DELETE 使用整数 `version/expectedVersion` 乐观锁。正常查询使用 `deleted_at IS NULL` 部分索引；
普通 API 不修改或删除非 MANUAL 来源账目。软删除重试仅在完整重验账本可见性、当前操作者权限、来源类型，
且墓碑版本恰为请求版本加一时幂等成功。能力字段只按当前操作者计算。
原因：后续可编辑字段不能反向改变原创建请求事实；乐观锁避免财务数据静默覆盖；来源边界和权限优先的
墓碑重试同时保证跨模块一致性、防枚举和网络重试安全。

## ADR-019：基础统计直接聚合业务日期并返回完整日序列

状态：已确认
决定：MVP 统计直接聚合未软删除 Entry，月份按用户时区与 `businessDate` 解释；趋势由服务端返回完整自然月
零值日序列。日均支出在当前月使用已过自然日、过去月份使用整月天数、未来月份为零，金额除法向下取整。
分类占比以整数万分比返回，成员支出按 Entry 创建人归属。
原因：统一首页、明细和统计口径，避免客户端补零或浮点重算产生差异，并在不引入汇总表的前提下满足基础统计。

## ADR-020：来源借贷存在同步账目时禁止删除

状态：已确认
决定：存在任何已同步普通账目的处理记录时禁止删除借贷；仅不存在同步账目的借贷可软删除，未同步处理记录一并软删除，关联普通账目永久保留且不得解绑。
原因：优先保证账目来源可追溯和财务历史稳定，避免删除来源业务后留下无法解释或被静默改写的普通账目。

## ADR-021：周期恢复不补暂停期间账期

状态：已确认
决定：周期规则恢复时不补暂停期间账期，从当天或之后符合规则的最近一期继续。
原因：暂停表示用户有意停止生成，恢复不应制造意外历史账目。

## ADR-022：短月份发薪日取月末

状态：已确认
决定：发薪日为 29 至 31 日且目标月份不存在该日期时，预计发薪日使用当月最后一天。
原因：保持每月发薪预测连续，并符合常见月末发薪口径。

## ADR-023：目标初始金额归创建者且完成状态可回退

状态：已确认
决定：攒钱目标初始金额归创建者；有效已存金额回落到目标以下时，从已完成恢复为进行中。
原因：贡献统计需要明确归属，完成状态应反映当前有效金额而非不可逆历史事件。

## ADR-024：Docker 为可选运行方式

状态：已确认
决定：保留 Docker Compose 作为一致的集成和容器部署方式，同时正式支持 Node 原生运行；PostgreSQL 与
Redis/Valkey 可由本机服务、独立服务器或云服务提供。原生模式使用同一 Prisma Schema、迁移、API、Worker
和环境变量，不引入 SQLite、内存 Redis 等行为不同的替代实现。生产原生网关默认只监听回环地址并置于
外层 HTTPS 反向代理之后。
原因：降低没有 Docker 环境时的开发和部署门槛，同时保持财务约束、队列语义与生产一致性。

## ADR-025：周期实例消耗期数、锚点日期与确认金额

状态：已确认
决定：月度/年度日期始终以规则开始日期为锚，短月取月末且后续不漂移；实例首次物化即消耗一个计划期数，
待确认实例后续跳过不入账且不再次改变期数；确认金额只覆盖当前实例，不回写规则默认金额。创建和确认分别
使用用户/操作/幂等键隔离，既有实例不因规则更新而改写；已有实例后锁定收支分类、频率、开始日期和生成方式。
原因：周期计划表达的是有限个计划账期，而不是必须成功入账的次数；锚点计算可避免 29 至 31 日和闰日漂移，
实例级金额既支持实际账单波动，也不会意外改变未来预算。

## ADR-026：周期 Worker 使用稳定账期任务与可操作通知接收人

状态：已确认
决定：周期 Worker 以规则 ID 和计划日组成稳定 BullMQ Job ID，消费时在数据库锁内复核当前计划日和创建人
时区；确认模式只通知当前仍可管理规则的活跃成员，即规则创建人与情侣账本 OWNER，并以接收人和实例唯一。
原因：队列投递只能提供至少一次语义，稳定任务键与数据库幂等共同防止重复财务写入；只通知可操作成员可避免
普通成员收到无法处理的提醒，同时为 TASK-021 提供可安全读取的通知事实。

## ADR-027：工资档案模板、月度复制与固定支出口径

状态：已确认
决定：MVP 每位用户最多一个有效工资档案，档案默认项目使用独立明细行保存；月度记录只接受显式项目或严格
复制紧邻上月记录，二者互斥。服务端从正金额月度项目计算应发、扣除和实发，档案/记录创建保存幂等请求哈希，
已到账记录不可修改。工资余额的固定支出仅包括工资周期内本人个人账本中来源为 `RECURRING_RUN` 的有效支出，
其他有效个人支出归日常支出。
原因：独立模板支持事务替换和未来多档案扩展，紧邻月份复制避免过期模板被意外复用，服务端汇总与幂等事实保护
工资数据一致性；以来源事实区分固定支出可审计且无需客户端分类猜测，同时严格保持工资和情侣数据隔离。

## ADR-028：工资到账采用独立幂等事实与双向来源约束

状态：已确认
决定：工资到账为不可逆终态操作，保存独立于工资记录创建请求的到账幂等键和规范载荷哈希。客户端显式选择
是否同步收入；选择同步时，只能在本人的有效个人账本和启用 `income.salary` 分类中创建
`INCOME + SALARY` 来源明细，金额和业务日期分别固定为实发工资与到账日；不选择同步时保持无来源明细。
工资状态、来源明细、到账幂等事实和审计在同一事务提交，并由记录级锁、用户级幂等唯一索引、来源唯一索引、
工资与明细双向延迟约束及不可变触发器共同保护。
原因：工资记录创建与到账是两个独立业务操作，复用创建幂等键会混淆重放语义；显式同步选择保留产品授权，
双向约束与原子事务可防止并发重复收入、单边关联和到账后财务事实漂移。

## ADR-029：工资统计使用工资月份和最近到账周期

状态：已确认
决定：年度汇总按本人未删除工资记录的 `salaryMonth` 归年并补齐 12 个月，月均实发以有记录月份数计算；
法定扣除与奖金按稳定项目代码聚合。工资余额使用用户时区今天之前最近到账记录，从到账日统计至下一个自然月
按档案发薪日计算的预计发薪日前；只聚合本人 PERSONAL 账本有效支出，`RECURRING_RUN` 为固定支出，其他
来源为日常支出。无到账记录返回空态，零剩余天数不计算日均金额。
原因：工资月份、到账日期和普通账目业务日期是三个不同事实；分别使用它们可避免跨年、补录、延迟到账和时区
边界串账。稳定代码不受显示名称变化影响，PERSONAL 账本与来源事实能够同时保证隐私和固定支出口径可审计。
