# 已知问题

| 编号 | 问题 | 影响 | 优先级 | 状态 |
|---|---|---|---|---|
| KI-001 | 正式产品名称已确认；Logo 尚未确认 | 品牌视觉 | 中 | 名称已解决 / Logo 待确认 |
| KI-002 | QQ 互联申请已通过；正式 App ID/App Key 和严格回调地址尚未进入部署环境 | 正式 QQ 登录 | 高 | 审核已通过 / 待安全部署配置与线上联调 |
| KI-003 | 当前设计图为低保真 | 最终视觉还原 | 中 | 待高保真 |
| KI-005 | 默认时区是否最终使用 `Asia/Shanghai` | 用户初始化与日期边界 | 中 | 待负责人确认 |
| KI-006 | 来源业务删除时关联账目的保留、解绑或删除策略 | 财务追溯与事务 | 高 | 已解决：ADR-020，存在同步账目时禁止删除来源借贷 |
| KI-009 | 周期跳过、恢复、补期和确认金额规则 | Worker 幂等与账目 | 高 | 已解决：ADR-021 冻结恢复不补期；ADR-025 冻结跳过消耗已物化期数、锚点日期和实例级确认金额 |
| KI-010 | 发薪日 29–31 日短月份与工资固定支出口径 | 工资周期与统计 | 高 | 已解决：ADR-022 冻结短月取月末；ADR-027 冻结个人账本周期来源支出为固定支出 |
| KI-011 | 情侣目标初始金额归属和完成后回退规则 | 攒钱汇总 | 高 | 已解决：ADR-023，初始金额归创建者且完成状态可回退 |
| KI-012 | 上传、异步导出、反馈和系统公告尚无批准的最小契约 | API/模型范围 | 中 | TASK-023 已批准有界同步 CSV；异步导出及其余能力后续设计 |
| KI-013 | Attachment、Setting、反馈和公告模型尚未批准 | 上传、设置和后台扩展 | 中 | AuthSession/RBAC 与最小后台已解决，其余后续设计 |
| KI-015 | 商标、域名、软著、应用商店名称和 `cn.duanap.siyu` 可用性未核验 | 正式发布 | 高 | 发布前确认 |
| KI-016 | 本机 Docker Redis 日志提示 `vm.overcommit_memory=0` | 低内存时后台持久化或复制 | 低 | 本地验收不阻断；部署主机设为 `1` |
| KI-017 | 生产邮件提供方尚未配置 | 正式密码重置邮件 | 高 | 队列与测试传输器已验证；生产任务明确失败为 `MAIL_PROVIDER_UNCONFIGURED` |

已解决：Prisma validate、OpenAPI lint/类型生成，以及“Docker/psql 环境缺失导致迁移和容器
验收待执行”均已在 TASK-000 通过。

TASK-004 已解决认证 Session、最小 RBAC、邮箱密码认证和密码重置数据契约；正式 QQ 与邮件服务
仍需部署凭据，不影响隔离测试和本地工程验收。

TASK-005 已通过 BR-COUPLE-010 至 BR-COUPLE-016、ADR-014 至 ADR-016 解决所有者退出/转移、
解散、情侣分类共享和邀请一次性凭证规则；实现、自动质量门与真浏览器视觉验收均已完成。PR #2
已以 Squash merge 合入 main，main push CI Run `29275012134` 全部通过，TASK-005 已正式关闭。

TASK-006 已通过 BR-CATEGORY-004 至 012、AC-CATEGORY-001 至 008 和 ADR-017 冻结系统模板账本内实例、
默认初始化、OWNER/MEMBER 分类权限、排序、启停和历史引用规则。实现、迁移/E2E、OpenAPI 74/74
和 Chrome 150 多尺寸双主题验收已通过；PR #4 已以 Squash merge 合入 main，功能提交
`25dcad0a29951ba4269e318423d5ebbf301857b3` 的 main push CI Run `29298535552` 全部通过，TASK-006
已正式关闭。

TASK-007 已通过 BR-ENTRY-007 至 015、AC-ENTRY-008 至 014 和 ADR-018 冻结不可变创建请求哈希、
乐观锁、权限优先软删除重试、来源维护边界和服务端能力计算。普通账目 API、第五迁移、E2E、OpenAPI
74/74 和 Compose/Nginx 链路已验证；PR #6 已以 Squash merge 合入 main，功能提交
`f6e579957535ccb5ea4ce06d9d4bb8368d7c994c` 的 main push CI Run `29305065285` 全部通过，TASK-007
已正式关闭。普通 API 仍不允许删除来源账目。TASK-008 记账客户端、TASK-009 首页/基础统计和 TASK-010
借贷 API 已经 PR #8、PR #9 合入 `main`。负责人已通过 ADR-020 解决 KI-006：借贷存在同步账目时禁止删除。
最新 main push CI Run `29571310880` 的 `quality`、`database`、`secret-scan` 全部通过，其中已执行借贷隔离
E2E、第六迁移部署和数据库约束脚本；TASK-008 至 TASK-010 正式关闭。TASK-011 已经 PR #10 合入
`main`，PR CI Run `29889775437` 与 main push CI Run `29889890113` 全部通过并正式关闭。TASK-012 已通过
ADR-025 完成周期规则/实例；功能提交经 PR #12 合入 `main`，PR CI Run `29894967989` 与 main push CI Run
`29895072247` 全部通过，TASK-012 正式关闭。没有新增已知业务阻断项。

TASK-013 的周期扫描、稳定 Job ID、多 Worker 幂等、指数退避、最终失败、确认通知和安全日志已经 PR #14
合入 `main`；PR CI Run `29898941240` 与 main push CI Run `29899089545` 全部通过，TASK-013 正式关闭。
当前无新增业务阻断。人工任务重试与审计已在 TASK-022 复用周期领域锁和幂等约束实现，不修改 TASK-013 的
自动扫描和失败不自动重跑语义。

TASK-014 周期移动端列表、创建、详情、编辑和实例操作已经 PR #16 合入 `main`；PR CI Run `29903360052`
与 main push CI Run `29903511196` 全部通过，正式关闭。没有新增业务、权限、API 或数据库阻断；低保真视觉
边界 KI-003 仍保留。

TASK-016 工资到账、可选 `income.salary` 收入联动、独立幂等事实、同事务审计和数据库双向不可变约束已经
PR #20 合入 `main`；PR CI Run `29912892385` 与 main push CI Run `29913079247` 全部通过并正式关闭。
没有新增业务、权限、隐私或数据迁移阻断项；宿主 Redis 6.0.16 的最低版本提示仍为本地非阻断环境差异，
Compose/CI 使用 Redis 7.4。

TASK-017 工资年度汇总和当前工资周期余额已经 PR #22 合入 `main`；PR CI Run `29917586181` 与 main push
CI Run `29917758079` 全部通过并正式关闭。年度统计、用户时区、短月、本人个人账本、固定/日常支出、负余额
和零剩余天数口径已由 ADR-029 固化；没有新增业务、权限、隐私或迁移阻断项。最终本地 E2E 首次执行时隔离
PostgreSQL 容器未运行，恢复容器并部署十次迁移后无代码改动即完整通过；宿主 Redis 6.0.16 的最低版本提示
仍为本地非阻断环境差异，Compose/CI 使用 Redis 7.4。

TASK-018 工资移动端三页已经 PR #24 合入 `main`；PR CI Run `29922038043` 与 main push CI Run
`29922213012` 全部通过并正式关闭。没有新增业务、权限、隐私、API、迁移或生产依赖阻断；真实 Chrome
多尺寸双主题和异常态验收通过。低保真视觉边界 KI-003、正式发布凭据与品牌资产问题继续保留。

TASK-019 已按 ADR-023 实现初始金额归创建者和完成状态可回退；目标/存入权限、幂等、并发汇总、成员退出
隔离和软删除留痕已通过迁移约束与完整 E2E。PR #26、PR CI Run `29929382178` 与 main push CI Run
`29929562773` 全部通过并正式关闭。没有新增业务、权限、隐私或数据迁移阻断；攒钱页面属于 TASK-020。

TASK-020 攒钱移动端列表、新建和详情已经 PR #28 合入 `main`；PR CI Run `29937823275` 与 main push CI
Run `29938160014` 全部通过并正式关闭。没有新增业务、权限、隐私、API、迁移或生产依赖阻断；首次本地全量
测试中既有周期快速提交用例出现单次时序抖动，单独复跑、最终完整复跑和两轮远程 `quality` 均通过。

TASK-021 本人通知稳定分页、未读计数、批量已读和移动端消息中心已经 PR #30 合入 `main`；PR CI Run
`29943627794` 与 main push CI Run `29943823960` 全部通过并正式关闭。没有新增业务、权限、隐私、迁移或生产
依赖阻断。标准移动端全套因 WSL/jsdom 串行环境初始化耗时约 7 分钟但 30/30 文件、99/99 项通过；迁移测试按
设计删除隔离 PostgreSQL 容器后，首次显式完整 E2E 在注册阶段返回 500，重建容器并部署 11 个迁移后无业务
代码改动即完整通过。宿主 Redis 6.0.16 的最低版本提示继续属于本地非阻断环境差异。

TASK-022 最小管理后台已经 PR #32 合入 `main`；PR CI Run `29968254365` 与 main push CI Run
`29968379326` 全部通过并正式关闭。首轮 PR CI 发现 OpenAPI 429 响应与共享生成类型不同步，补齐生成产物后
未弱化任何门禁即通过。当前没有新增业务、权限、隐私或迁移阻断；WSL 禁用 Windows 可执行互操作，导致后台
三尺寸、双主题真浏览器矩阵未执行，该缺口已如实保留，组件测试、构建和接口 E2E 未冒充视觉验收。

TASK-023 当前用户可见账本账目与本人工资的有界同步 CSV 已经 PR #34 合入 `main`；PR CI Run
`29970521966` 与 main push CI Run `29970632424` 全部通过并正式关闭。权限固定到有效成员或本人，整数分
精确输出、公式注入防护、10000 行上限、限流、脱敏审计、真实 PostgreSQL/Redis E2E 和 Chrome 实际下载
均已验证。异步导出任务、对象存储、上传、反馈和公告模型仍未获批准，不属于本任务；当前没有新增阻断。

## 2026-07-17 工具说明

- Windows 本地 Redocly CLI 在成功验证 OpenAPI 后退出时可能触发 libuv `UV_HANDLE_CLOSING` 断言；OpenAPI 74/74 覆盖脚本可独立通过，Linux GitHub CI 作为最终门禁。该问题不影响 API 契约内容。
- Git Bash 未加入默认 PATH，清单和文档脚本需使用 `C:\Program Files\Git\usr\bin\bash.exe -lc`；本次生成与复核均已通过。
